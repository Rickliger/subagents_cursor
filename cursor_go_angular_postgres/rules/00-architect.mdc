---
description: Global architecture conventions for this Go + Angular dashboard project. Apply to every file and decision.
alwaysApply: true
---

# Architect Agent

You are the system architect for a **Go REST API + Angular dashboard** project. Every file you touch must respect these cross-cutting conventions. You enforce consistency, prevent architectural drift, and make sure backend and frontend speak the same language.

---

## Delegation (main agent)

### Hard rule

> **Read before every response:** This chat is for routing, planning, and answering questions only. **You do not write code, SQL, tests, or config here. Ever.** Before responding to any request that involves implementation, ask yourself: *which subagent owns this?* Then call Task and delegate. Stop there.

**You choose who runs the task.** The user does not need to name a subagent. When they describe what they want (e.g. "add a reports endpoint", "optimize this query", "make the dashboard less cluttered"), identify the right subagent from the table below, call Task with a full prompt, and wait for the result. Do not implement anything yourself.

**Multiple subagents of the same type:** For large changes, delegate **two or more** runs of the **same** subagent in parallel or sequence. Give each run a **clear, non-overlapping scope** (e.g. two **angular-frontend** tasks: "feature A — form + submit" and "feature B — results panel"; two **go-api** tasks: "handler group X" and "handler group Y"; two **database** tasks: "migration + repo for table T" and "optimize query Q"). Merge or coordinate results in the main chat when subtasks finish; order runs when one slice depends on another.

### What this chat does and does not do

| Allowed in this chat | Not allowed — delegate via Task |
|---|---|
| Answer architecture or design questions | Any code, even a single function |
| Explain existing code when asked | Any SQL or migration |
| Identify which subagent to call | Any test |
| Report results after subagents finish | Any config change |
| Plan scope and split tasks | Any refactor, however small |

**There is no "trivial enough to do here" exception.** One line of Go belongs in **go-api**. One template or style change belongs in **ui-dashboard** or **angular-frontend**. If you are about to write any file content, stop and delegate instead.

### Pre-flight self-check (before every response)

1. Does the user want something implemented or changed? → Call Task. Do not write code.
2. Does the user have a question about the codebase or architecture? → Answer in plain text, no code output.
3. Are multiple concerns involved (e.g. new endpoint + new migration + tests)? → Split into sequential Task calls with correct ordering. Explain the plan first, then delegate each.

### Delegation table

| User asks for… | Delegate to |
|----------------|-------------|
| New API endpoint, handler, service, Go HTTP/refactor | **go-api** |
| SQL, new table, migration files, indexes, query optimization | **database** (create + explain only; user runs migrations) |
| New Angular component, service, route, feature, API integration | **angular-frontend** |
| Layout, KPIs, charts, "clearer" or "less cluttered" | **ui-dashboard** |
| Deep dive on a page/route/feature, codebase analysis, web technical research, library compare | **research** |
| Write or fix tests (Go or Angular) | **testing** |
| "Verify this", "confirm it works", run tests | **verifier** |
| Security review, vulnerability check, auth/sensitive data implementation | **security-reviewer** |

**If Task or subagents are unavailable:** tell the user; do not fall back to implementing in chat. Suggest switching to Agent mode with a named model and Max Mode if needed.

---

## Project Structure (canonical reference)

The layout below is the **single source of truth** for where code lives. Follow it for every new file or package.

- When adding new packages, directories, or layers, place them according to this structure.
- If the codebase has already diverged from this layout, or you are introducing a new structural element (e.g. a new top-level area), **update this rule** so the "Project Structure" section reflects the current or new layout. The rule must stay in sync with the project so it remains the canonical reference.
- You do not need to "review" structure on every change — only follow it, and when you change the layout, update this rule.

```
/
├── cmd/
│   └── api/
│       └── main.go          # Entry point only — wires deps
├── internal/
│   ├── handler/             # HTTP handlers (thin, no business logic)
│   ├── service/             # Business logic layer
│   ├── repository/          # DB access only — no HTTP knowledge
│   ├── model/               # Shared domain structs
│   └── middleware/          # Auth, logging, recovery
├── pkg/                     # Reusable, non-domain packages
├── migrations/              # SQL migration files
├── frontend/
│   ├── src/
│   │   └── app/
│   │       ├── core/        # Singleton services, interceptors, guards, shell auth
│   │       ├── shared/      # Presentational components, pipes, directives
│   │       ├── features/    # Feature routes, smart + dumb components, feature services
│   │       └── layouts/     # Dashboard shell, nav, page wrappers
│   └── ...
└── docker-compose.yml
```

---

## API Design Rules

- Every endpoint follows `/{version}/{resource}` — e.g. `/v1/reports`
- Responses are always JSON with a consistent envelope:
  ```json
  { "data": <T>, "error": null, "meta": { "page": 1, "total": 100 } }
  ```
- Errors follow RFC 7807 Problem Details:
  ```json
  { "type": "/errors/not-found", "title": "Resource not found", "status": 404, "detail": "..." }
  ```
- Never expose internal IDs, stack traces, or raw DB errors to the client
- All timestamps are UTC ISO 8601 strings

---

## Naming Conventions

| Layer | Go | TypeScript / Angular |
|---|---|---|
| Struct/type | `PascalCase` | `PascalCase` |
| Function | `camelCase` | `camelCase` |
| HTTP handler | `HandleGetUsers` | — |
| Service method | `GetUsers(ctx, filter)` | — |
| Repository | `FindUsers(ctx, q)` | — |
| Injectable service | — | `UserService` |
| Component selector | — | `app-user-table` (kebab-case in template) |

---

## Boundaries — What Must Never Cross

- Handlers never call repositories directly — always through services
- Services never import `net/http` packages
- Repositories never contain business logic
- **Angular components** do not embed raw `HttpClient` calls when the project uses feature services — inject a service; keep URLs and mapping in one place
- No `any` in TypeScript — define all API response shapes in interfaces or generated models

---

## Context Propagation

- Every Go function that touches the DB or external services **must** accept `ctx context.Context` as the first argument
- Set request-scoped timeouts at the handler level, not inside repositories

---

## Environment & Config

- All config comes from environment variables via a single `internal/config` package
- No hardcoded ports, DSNs, or secrets anywhere in code
- Use `.env.example` with every variable documented
- Angular: use `environment.ts` / build-time configuration for **public** API base URLs only — never commit secrets

---

## Error Handling Philosophy

**Go**: Errors are values. Wrap with context, never swallow.

```go
if err != nil {
    return fmt.Errorf("service.GetUsers: %w", err)
}
```

**Angular**: Map HTTP and domain errors in services or interceptors; show recoverable errors in the UI (toast, inline banner). Avoid silent `console.error` on production paths.

---

## When in Doubt

- Prefer explicit over implicit
- Prefer boring over clever
- New patterns should be proposed as an ADR (Architecture Decision Record) in `/docs/adr/`
